# robot_interfaces

`robot_interfaces` 是第 2 周第 3 节的自定义接口包。

它只定义 ROS 2 接口，不实现节点逻辑。

当前包含：

```text
msg/TargetDetection.msg
srv/SetGoal.srv
```

本节讲义：

```text
WEEK_02_03_CUSTOM_INTERFACES_AND_REVIEW.md
```
